/*import { compareHash } from "./login.js";

const d = document

const $btn = d.querySelector("button"),
 $trans = d.querySelector("#transition")

$btn.addEventListener("click", e => {
    $trans.classList.add("active", "bg-color")
})

compareHash()*/